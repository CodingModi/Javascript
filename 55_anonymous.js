// Store an anonymous function in variable 
let maximum = function(a, b) {

    // Compare two numbers
    if (a > b) {

        // Return first number
        return a;

    } else {

        // Return second number
        return b;

    }

};

// Display the maximum number
console.log(maximum(50, 35));